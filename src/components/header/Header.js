import React from "react";
import Form from "../form/Form";
import Nav from "../nav/Nav";

const Header = () => {
  return (
    <header id="header">
      <Nav />
      <Form/>
    </header>
  );
};

export default Header;
